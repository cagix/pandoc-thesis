name: Build using the Eisvogel template

on:
  # push on master branch
  push:
    branches: [master]
  # triggered manually
  workflow_dispatch:
  # triggered by pull requests
  pull_request:

jobs:
  compile:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: make eisvogel
      - uses: actions/upload-artifact@v4
        with:
          name: thesis_eisvogel
          path: thesis.pdf
          overwrite: true
